/**
 * @param {string} s
 * @return {boolean}
 */
function isValid(s){
    const stack = [];
    const map = {
        ')':'(',
        ']':'[',
        '}':'{'
    };

    for()
}
// var isValid = function(s) {
    
// };

// module.exports = { isValid };


